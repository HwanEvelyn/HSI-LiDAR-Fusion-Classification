# Install a CUDA-enabled PyTorch build separately before installing this file.
# Example for NVIDIA GPU on Windows:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# Then install the remaining project dependencies:
#   pip install -r requirements.txt

numpy>=1.24
scipy>=1.10
matplotlib>=3.7
scikit-image>=0.22
tifffile>=2024.2
